const actions = {
  //   async nuxtServerInit({ commit }, { $content }) {
  //     const snippets = await $content('snippets').fetch()
  //     console.log(snippets)
  //     await commit('SET_SNIPPETS', snippets)
  //   },
}

export default actions
